# v0.9.4 Google Context Transport

- Prefer direct service-account reads for Google Sheets; retain webhook as read fallback and preserve existing webhook-first writes.
- Parse GOOGLE_SERVICE_ACCOUNT_JSON safely as normal JSON, double-encoded JSON, or base64 JSON.
- Reuse the parser for Calendar service-account access.
- Surface safe non-JSON webhook diagnostics without response body or credentials.
- No AI/model calls are added by context diagnostics.
