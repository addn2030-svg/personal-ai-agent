# بيانات تشغيل شخصية — لا تُرفع أبدًا (خصوصية)
data/state.json
data/audit.jsonl
data/backups/
data/.state.lock
data/.timing.lock
logs/
data/inbox.csv
data/inbox-imported-*.csv
data/.manager-markers.json
reports/
uploads/
# Connector credentials / tokens — local only
secrets/
*.credentials.json
*.token.json
.env
.env.*
# عام
__pycache__/
*.pyc
.DS_Store
data/.telegram*

# معرفة خاصة غير منشورة — حقول الاتصال الحقيقية (لا تُرفع أبدًا)
knowledge.private/
